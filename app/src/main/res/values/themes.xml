<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.OneVault" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen Theme -->
    <style name="Theme.OneVault.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black_default</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.OneVault</item>
    </style>
</resources>